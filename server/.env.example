# Port (Railway sets this automatically)
PORT=3001

# Comma-separated list of allowed frontend origins
CORS_ORIGIN=https://your-app.vercel.app

# Directory where rugmap.db is stored
# On Railway: set to the mount path of your persistent volume, e.g. /data
DATABASE_DIR=/data
